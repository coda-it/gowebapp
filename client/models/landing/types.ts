export type PutLandingAction = {
  type: string;
  input: string;
};

export type State = {
  input: string;
  id: string;
};

export type Action = {
  type: string;
  input: string;
  id: string;
};

